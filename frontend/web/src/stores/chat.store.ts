'use client';
import { create } from 'zustand';
export type ChatMode = 'single' | 'compare' | 'battle' | 'consensus' | 'router';
export interface Message { id: string; conversationId: string; role: 'user' | 'assistant' | 'system'; content: string; model?: string; provider?: string; costUsd?: number; latencyMs?: number; createdAt: Date; }
export interface Conversation { id: string; userId: string; title: string; mode: ChatMode; model?: string; totalTokens: number; totalCost: number; createdAt: Date; updatedAt: Date; }
interface ChatState { conversations: Conversation[]; activeConversationId: string | null; messages: Record<string, Message[]>; streamingContent: string; isStreaming: boolean; selectedModels: string[]; chatMode: ChatMode; setConversations: (c: Conversation[]) => void; addConversation: (c: Conversation) => void; setActiveConversation: (id: string | null) => void; setMessages: (id: string, m: Message[]) => void; addMessage: (id: string, m: Message) => void; appendStreamChunk: (c: string) => void; setStreaming: (s: boolean) => void; clearStream: () => void; setSelectedModels: (m: string[]) => void; setChatMode: (m: ChatMode) => void; }
export const useChatStore = create<ChatState>((set) => ({ conversations: [], activeConversationId: null, messages: {}, streamingContent: '', isStreaming: false, selectedModels: ['gpt-4o'], chatMode: 'single', setConversations: (conversations) => set({ conversations }), addConversation: (conv) => set((s) => ({ conversations: [conv, ...s.conversations] })), setActiveConversation: (id) => set({ activeConversationId: id, streamingContent: '' }), setMessages: (convId, messages) => set((s) => ({ messages: { ...s.messages, [convId]: messages } })), addMessage: (convId, message) => set((s) => ({ messages: { ...s.messages, [convId]: [...(s.messages[convId] ?? []), message] } })), appendStreamChunk: (chunk) => set((s) => ({ streamingContent: s.streamingContent + chunk })), setStreaming: (isStreaming) => set({ isStreaming }), clearStream: () => set({ streamingContent: '' }), setSelectedModels: (selectedModels) => set({ selectedModels }), setChatMode: (chatMode) => set({ chatMode }) }));